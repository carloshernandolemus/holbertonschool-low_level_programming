#include "holberton.h"
/**
 *mul - Is the function
 *@a: Is the first number variable function
 *@b; Is the second number variable function
 *Return: Return the function
 */
int mul(int a, int b)
{
	return (a * b);
}
