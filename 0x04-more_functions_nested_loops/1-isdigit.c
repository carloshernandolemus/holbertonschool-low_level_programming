#include "holberton.h"
/**
 *_isdigit - Is the function
 *@c: Is the variable function
 *Return: Return the function
 */
int _isdigit(int c)
{
	if (c > 47 && c < 58)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
